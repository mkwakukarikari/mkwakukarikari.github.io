# Images

Place site images here.

- `profile.jpg` — Profile photo shown on the homepage. Recommended: square crop, at least 600x600 pixels.

If `profile.jpg` is missing, the homepage automatically shows "KM" initials in place of the photo, so the site won't break.
