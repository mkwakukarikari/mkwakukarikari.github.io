# Files

Place downloadable documents here, like:

- `MANU_CV.pdf` — Linked from the CV page download button.

If `MANU_CV.pdf` does not exist, the download button on `cv.html` will produce a 404. Either upload a CV PDF here or remove the download button from `cv.html`.
